# Per-test knobs for ci/tasks/run-tests.sh. Sourced, so plain assignments only.

# The test pulls redis:7-alpine through testcontainers on every run, by design -
# that is what it is testing. Docker's "Pulling from" output would otherwise read
# as a cache miss.
SKIP_CACHE_CHECK="true"
